/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tripwayholidays.in', 'images.unsplash.com', 'tripwayholidays'],  // Add your domain(s) here
    },
};

export default nextConfig;
